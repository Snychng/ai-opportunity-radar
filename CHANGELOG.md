# 版本变更

## 4.4.0 — 2026-09-16

- 新增任务优先观察：`research/resume --observations-file` 可独立记录用户、任务、触发事件、现有做法、期望结果和具体产物，不要求先知道产品名、收费对标或 AI 方案。产品上下文与解决方案假设不改变需求身份；旧版观察和报告保持可读。
- 默认发现增加事件、实际操作、工作产物、正向动机和能力变化五种入口；已有用户观察可生成带原文引用的后续查询，以及可用于下一轮研究的意图文件。生成查询不代表已经检索或发现新需求。
- 收藏、纪念、分享、创作、完成和技能提升作为明确行为保留；一次性交付、人工辅助服务、插件、工作室工具和订阅软件作为独立假设比较，不通过更换产品或交付形态增加需求数量。
- 阅读容量按行业、任务、来源和证据角色分配，增加主包之外的有界补读包及可继续阅读的清单；原文不因未进入主包而消失，生成阅读包不等于已经审阅。
- 研究覆盖与流程完成分开：没有定义有效研究范围、仍有未审材料或缺少必要研究维度时，不再把完成归档当成市场研究充分。
- 保留 4.3.1 的评论分页、恢复和预算边界；后续网页意图不自动执行付费请求，不放宽正式 A/B/R 门槛，也不公开用户原话和内部路径。

## 4.3.1 — 2026-09-16

- 修复产品搜索计划缺少付费发现目标的问题：从已验证的宿主意图保留目标，执行继续要求显式预算和实时账户预检。
- 评论分页 cursor 可接受供应商返回的长游标，上限为 4096；其他参数保留原限制。
- 预检失败后的恢复以执行账本是否实际登记批次为准，避免仅凭计划文件误判；已保存响应继续复用，不重买失败或结果未知的页面。
- 解析器升级至 2.2.1：统一识别 X 两种评论接口的 id / tweet_id，去重并排除原帖和独立转引帖；分页计数与离线重解析使用相同规则。
- 已完成一次受预算约束的三平台在线采集，验证小红书和 X 多页评论、抖音评论读取；不声明抖音深分页、全量评论或用户购买身份已验证。实抓原文、账户数据和本地运行路径不包含在发行包中。

## 4.3.0 — 2026-09-16

- 新增评论优先的产品研究：`research --products-file` 从产品、别名和实际任务生成小红书、抖音、X 的体验、持续使用与替代/退款查询，支持最多 100 个意图。
- 新增 `resume --comments-file --max-cost-usd`：单批选择最多 30 个相关帖子，采集详情、评论分页和小红书/抖音子回复；X 同时查询相关及最新回复。页数、回复线程、评论目标及总请求均有明确容量。
- 分页状态与游标持久化，按原生评论 ID 去重，记录每页产出、停止原因和未继续请求数；预算中断后 `--resume-batch` 恢复，复用已保存响应，不自动重买失败或未知页面。
- 新增用户观察 OBS 与需求簇 NEED：可以先保存确切原文和需求，不要求已有收费对标、AI 方案或 MVP；使用/购买声称/退款声称/求推荐与推广、官方回复、疑似刷评和未知材料分开，均不自动认定真实身份或成交。
- 研究报告展示本轮实际评论覆盖、用户观察和需求簇；正式 A/B 门槛保持原有要求，公开数据只增加观察统计，不公开未复核评论、用户信息或产品需求正文。
- 新研究默认读取免费社区评论，可用 `--no-include-comments` 关闭；解析器升级至 2.2.0，支持 X 原生回复 ID、时间线游标及子回复父级绑定，保留原始响应和固定修订引用。
- 修复供应商紧凑时区偏移（如 +0000）在 Python 3.10 下被误标为低置信日期的问题，保持精确时间与原始值。
- 验证范围为合成响应、离线全流程、预算/中断恢复和发行 CI；没有使用付费实时抓取来声明三平台在线覆盖或全量评论完整性。

## 4.2.0 — 2026-09-14

- 新增持久化批量语义审阅队列，按行业领单、限时租约、失败恢复及确切修订提交；未知和缺正文不会冒充已审阅。
- 新增真实材料检索质量基准，分别记录查询相关性、用户/供方身份、任务/付款/推广信号，保留未标注分母和样本限制。
- 网站数据及 CLI 线索共用当前引用有效性判断；增加可选的复核日期、到期状态与补证任务，兼容 public 1.0.0。
- 增加跨运行的日/月/累计预算、历史估算幂等导入与持续任务首错暂停；请求账本和全局预算在同一 SQLite 事务中保守占用费用。
- 早期 LEAD 可直接记录 AI 与既有方法的对照实验计划，预先定义任务、指标、样本及验收条件，实测值必须绑定证据。
- 提供可直接读取 public.v1.json 的网站消费端示例，包含六方向筛选、搜索、详情、来源、归档状态和复核日期。

## 4.1.0 — 2026-09-14

- 默认研究收窄到用户选定的六个方向，按行业任务规划中英文查询及网页核验，消除 HN/GitHub 开发者词作为唯一自动发现入口的偏向；目录可配置扩展。
- 增加一次性显式预算 `resume --discover`，实时估价、均衡选取请求、记录无价格端点；沿用整轮累计费用和未知请求保护。
- 证据包按行业、来源、角色平衡选择，另附完整索引及报告截断诊断；计划、请求、材料、相关性、人工核验分别计数。
- 普通用户的持续使用、创作、学习及分享行为纳入需求信号；正式候选保留原门槛，早期需求写入独立探索线索及可恢复历史。
- 新研究要求明确 AI 相对原有方法的增量假设，报告保持假设、研究资格和客户验证的区分。
- 修复 Reddit 搜索时间范围枚举与来源相对日期入库失败；解析可复用已付费响应，无需重新购买。
- 原生帖子/评论身份、解析派生集合与语义复核分别持久化；网站公开契约、Schema、TypeScript 和原子全站目录统一导出。

## 4.0.0 — 2026-09-10

本次大版本将默认研究路径升级为可恢复的研究引擎。产品版本为 4.0.0；数据与评分协议继续使用 3.0，旧 CLI 入口和历史显式 ID 保持兼容。

- 新增 `research / resume / inspect` 文件化研究流程，保留 Agent 判断环节的证据包、输入模板和下一步动作；独立运行支持同日补证与中断恢复。
- 查询计划支持宿主结构化意图，按实际 HTTP 参数去重并保留全部意图引用；来源目录和网页证据导入区分配置、检索意图与真实覆盖。
- 采集质量增加多语言匹配、日期窗口和部分失败统计；公开社区支持按需评论深挖与旧 Issue 的近期活动。
- 付费请求逐条保存执行状态，整轮跨批预算累计；成功结果可复用，结果未知的请求不自动重买。
- 证据 JSONL 日志配合可重建检索索引，保留修订与历史截止；商业主张检查原文定位和引用修订，不冒充自动语义核验。
- 结构化 `report.json` 统一清单、摘要与正式提交，报告摘要绑定状态记录；保留原有 Markdown 校验及低层命令。
- 新业务逻辑分层到 `src/aor`，兼容脚本与稳定安装入口保持可用；新增离线研究质量评估，运行时仍仅依赖 Python 标准库。
- 自动生成的新 BENCH 身份不再包含价格，显式历史 ID 继续沿用；已有研究状态不做批量迁移。

升级后请重新读取 `SKILL.md` 和研究工作流；新运行优先使用 `research / resume / inspect`。既有手动采集链路继续可用，新证据库与运行目录按需建立，无需清空历史状态。付费补证共用同轮请求账本；结果未知的历史请求不自动重新购买。


## 3.2.2 — 2026-09-10

- `aor` 首页增加五行圆角几何 AOR 青蓝字标，并按分组展示产品名、版本、技能与常用命令。
- `aor` 与 `aor skills` 的日常文本突出技能名称、版本和异常提示，技能列表补充说明；提交 SHA 与绝对安装路径可通过 `aor --json`、`aor skills --json` 或 `aor doctor` 查看。
- 窄终端内容自动换行；非 TTY 或 `TERM=dumb` 使用无 ANSI、无字标的紧凑文本；`NO_COLOR` 非空时禁用颜色并保留普通 TTY 布局，输出编码不支持 UTF-8 符号时使用文字降级。
- JSON 输出、更新提醒、`doctor` 诊断和研究数据格式保持兼容；显示能力仅使用 Python 标准库，无新增运行时依赖。

## 3.2.1 — 2026-09-10

- 新增 `aor doctor --quiet`，用于日常技能启动检查：已是最新、没有确认的新版本或网络未知时不输出例行版本提示，本地致命错误仍可见。
- 发现稳定更新时，统一显示真实新版本号和 `aor update` 命令；业务提示继续使用标准错误，保留业务 JSON 输出。
- Skill 明确要求 Agent 不复述“已是最新”等例行状态，同一研究任务中相同新版本只转述一次；主动 `doctor` 与 JSON 诊断保留完整信息。
- 无参数概览仅在缓存确认有新版本时附加更新提醒；安装、缓存和研究数据格式保持兼容。

## 3.2.0 — 2026-09-10

本版本增加本项目技能的查看、环境诊断和稳定版本管理，让具备文件与命令执行能力的 AI Agent 使用同一套安装及更新入口。

### 新增

- `aor` 查看当前安装概览，`aor skills` 列出本项目清单登记的技能，`aor doctor` 检查环境与稳定更新，`aor update` 显式升级受管安装；管理命令支持 JSON 输出。
- `install` 从官方最新稳定 Release 对应的 Git 标签创建独立版本目录和真实命令；`install --source` 支持显式安装干净的本地官方来源 Git 快照。
- 根技能和研究命令加入更新预检：成功缓存 24 小时，网络失败缓存 5 分钟为未知，离线检查不联网、不写更新缓存。有更新只提示，不自动替换正在使用的技能。
- 安装与运行使用独占/共享锁，候选校验通过后原子切换 `current`，保留旧版本目录，并保护本地修改、安装来源和已有同名命令。
- 补充跨 Agent 接入、安装更新、状态含义和更新后重新读取技能的说明。
- 稳定标签触发双 Python 版本验证和自动发行，验证及发布固定到同一提交；发行包包含源码归档、提交清单和 SHA-256 校验文件。

### 兼容性与边界

- 保留 `python3 scripts/radar.py` 和原有独立业务脚本；预检提示使用标准错误，不混入业务 JSON。
- 只管理 AI Opportunity Radar 及清单登记的本项目子技能；当前仅登记一个根技能，同一 Release 统一版本。
- Python 3.10+、macOS/Linux，运行时保持仅使用标准库；安装和升级需要 Git。
- 研究数据仍使用独立的 `AI_OPPORTUNITY_RADAR_HOME`，`schema_version` 保持 `3.0`，本版本不迁移既有数据。
- 更新检查比较版本号，不将相同版本号当作相同 Git 提交的证明。纯 Skill 文档也不能强制所有宿主执行预检，更新后需要重新读取技能。
- 当前没有预发布渠道、子技能独立升级、回滚命令或原生 Windows 锁实现。

### 验证范围

自动化测试覆盖目录外 CLI 调用、兼容业务入口、技能注册、安装身份、缓存与离线行为、发布响应校验、独立版本快照、修改保护、运行锁和安装失败时的现有版本保护。网络响应使用受控测试替身；真实 GitHub 发布、制品和在线安装的结果以对应 Release 工作流及发行记录为准，不由本地测试替代。

使用说明见 [安装与更新](references/installation-updates.md)；完整发行列表见 [GitHub Releases](https://github.com/Snychng/ai-opportunity-radar/releases)。
